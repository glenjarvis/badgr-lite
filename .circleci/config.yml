# Python CircleCI 2.1 configuration file
#
jobs:
  build:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run: echo 'hello'
  test:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run: echo 'world'
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
